import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginVertical: 8,
  },
  emptyText: {
    color: '#888',
  },
  label: {
    marginBottom: 4,
  },
  picker: {
    backgroundColor: '#f0f0f0',
    borderRadius: 8,
  },
});
